import React from 'react';
import { Text, View } from 'react-native';

const WatchListAdd = (props) => {
  return (
    <View>
      <Text>관심목록</Text>
    </View>
  );
};

export default WatchListAdd;
